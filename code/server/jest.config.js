// eslint-disable-next-line no-undef
module.exports = {
    preset: "ts-jest",
    testEnvironment: "node",
    testMatch: ["**/**test**/**/*.test.ts"],
    reporters: ["default"],
    testTimeout: 100000,
};
